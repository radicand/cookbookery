Hasura GraphQL Engine has been deployed.

{{- if .Values.ingress.enabled }}
Access the Hasura API at:
  http{{ if .Values.ingress.tls }}s{{ end }}://{{ .Values.ingress.host }}
{{- else if contains "ClusterIP" .Values.service.type }}
Access the Hasura API by running:
  kubectl --namespace {{ .Release.Namespace }} port-forward svc/{{ include "cookbook-hasura.fullname" . }} {{ .Values.service.port }}:{{ .Values.service.port }}
  Then visit http://127.0.0.1:{{ .Values.service.port }}
{{- end }}

{{- if not .Values.envFrom }}

WARNING: No envFrom configured. Hasura requires at minimum:
  - HASURA_GRAPHQL_DATABASE_URL
  - HASURA_GRAPHQL_ADMIN_SECRET
Set envFrom to reference a Secret or ConfigMap containing these values.
{{- end }}
